db:
  path: diun.db

watch:
  workers: 20
  schedule: "0 0 8 ? * SAT"
  firstCheckNotif: false
  compareDigest: true

notif:
  pushover:
    token: replaceme
    recipient: replaceme

providers:
  docker:
    watchByDefault: true
    watchStopped: false